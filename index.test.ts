import { BrexExpenses } from "./index";

describe("brex-expenses-typescript-sdk", () => {
    it("initialize client", async () => {
        const brexexpenses = new BrexExpenses({
        });
    });
});
